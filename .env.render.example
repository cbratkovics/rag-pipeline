# Render Environment Variables
# Copy these to your Render dashboard: Settings > Environment

# Application
APP_NAME=rag-pipeline-ragas
APP_ENV=production
DEBUG=false
LOG_LEVEL=INFO

# API Configuration
API_HOST=0.0.0.0
API_PORT=$PORT  # Render provides this
CORS_ORIGINS=["https://your-app.vercel.app","https://*.vercel.app"]

# Redis (Auto-configured by Render)
REDIS_URL=$REDIS_URL  # Provided by Render Redis service

# Vector Store
VECTOR_STORE_TYPE=chromadb
CHROMA_PERSIST_DIR=/opt/render/project/.chroma

# OpenAI Configuration (REQUIRED)
OPENAI_API_KEY=sk-...  # REQUIRED: Set in Render dashboard
OPENAI_MODEL=gpt-3.5-turbo

# Embedding Model
EMBEDDING_MODEL=all-MiniLM-L6-v2

# RAG Configuration
CHUNK_SIZE=512
CHUNK_OVERLAP=50
MAX_CONTEXT_LENGTH=2048

# Hybrid Search Weights
HYBRID_WEIGHT_BM25=0.5
HYBRID_WEIGHT_VECTOR=0.5

# Monitoring
PROMETHEUS_ENABLED=true
PROMETHEUS_MULTIPROC_DIR=/tmp/prometheus_multiproc

# Protocol Buffers (Required for ChromaDB)
PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python
